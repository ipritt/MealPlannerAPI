MealPlanner Notes

-----Current state - Recipe and Ingredient CRUD operations are working. Still needs validations, error handling and unit tests. But, with those working, for now, 
I'm going to hop over to creating an Angular UI that can exercise these endpoints.-----

-----Future - Will be adding lookup tables for Units and possibly Category. Although, I am reconsidering even having Category and leaning toward a Tag system.
	Will also be adding a Users table so I can start setting up authentication and "Inventory" functionality.

PUT returns 204 NoContent
POST returns 201 and inserted model
DELETE returns 

Questions

Decisions
-Consider paging for GetAll endpoints
-Consider handling race conditions for updates and deletes


ToDos
-More testing and think of addition rules/validations
-Handle all possible exceptions and return appropriate status codes
-Start thinking about how to handle authentication and authorization
-How to handle blob storage and retreival, possible to update? For recipe pdf and txt uploads
-Centralize error handling even more, each service method has nearly identical code for it
-Handle validations in separate validation layer or service, and return a list of errors instead of throwing exceptions


Current issues


DONE --GetAllInventory isn't returning values for Name or Unit - Needed to return inventory.Ingredient.Name and inventory.Ingredient.Unit instead of inventory.Name and inventory.Unit in MapResponseFromEntity
DONE --Same for GetInventoryById - Needed to return inventory.Ingredient.Name and inventory.Ingredient.Unit instead of inventory.Name and inventory.Unit in MapResponseFromEntity
DONE --Concering the two points above, POSTing an invetory item with values for Name and Unit returns those, so they're just not being set somewhere
DONE --What should be returned for a successful Post? DONE - 201 and model
DONE --When all crud oprations are functional, need to include some restrictions like when and what can be deleted, prevent adding an identical record, etc
DONE --GetIngredientsById isn't returning UsedByRecipes data
DONE --Move mappings into DTOs or separate mapping class, maybe look at AutoMapper -Moved mapping from services to models for now
DONE --Recipe ingredient quantity not working - Created seperate RecipeIngredientResponseDTO to return Quantity for recipe ingredients and InstockAmount for Ingredient
DONE --Ingredient InStockAmount not working - Created seperate RecipeIngredientResponseDTO to return Quantity for recipe ingredients and InstockAmount for Ingredient
DONE --Recipe ingredients UsedByRecipes is borked - the first recipe is only seeing itself, so all ingredients are only used in the one recipe, second recipe is correct
		- Needed to get all recipes then pass into Mapping function because it only had the recipes for each iteration
DONE --When adding a new ingredient, should require an amount for InstockAmount 
	(could be zero, but if going to be used in a recipe, needs to have at least as much as the recipe requires, but that's a recipe rule, not an ingredient rule) 
	----This is not true. In the near future, InStockAmount is going to live on what is now the Inventory table, which will end up being a join table bewtween Users(doesn't exist yet) and Ingredients.
DONE --Recipe Add is adding ingredients even if they already exist - Hiding or removing Ingredients from RecipeDTO and only having IngredientId forgien key.
