MealPlanner Notes

PUT returns 204
POST returns 201 and inserted model

Questions/ToDos???
-Move mappings into DTOs or separate mapping class, maybe look at AutoMapper
-More testing and think of addition rules/validations
-Consider paging for GetAll endpoints
-Consider handling race conditions for updates and deletes
-Handle all possible exceptions and return appropriate status codes
-Start thinking about how to handle authentication and authorization


Current issues getting crud operations working



DONE --GetAllInventory isn't returning values for Name or Unit - Needed to return inventory.Ingredient.Name and inventory.Ingredient.Unit instead of inventory.Name and inventory.Unit in MapResponseFromEntity
DONE --Same for GetInventoryById - Needed to return inventory.Ingredient.Name and inventory.Ingredient.Unit instead of inventory.Name and inventory.Unit in MapResponseFromEntity
DONE --Concering the two points above, POSTing an invetory item with values for Name and Unit returns those, so they're just not being set somewhere
DONE --What should be returned for a successful Post? DONE - 201 and model
DONE --When all crud oprations are functional, need to include some restrictions like when and what can be deleted, prevent adding an identical record, etc
DONE --GetIngredientsById isn't returning UsedByRecipes data
